FROM ichiran-main:latest   
WORKDIR /app

# Install Python + FastAPI + Uvicorn
RUN apt-get update && apt-get install -y python3 python3-pip \
    && pip3 install fastapi uvicorn

COPY api/ /app/

EXPOSE 8000

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
